# color_extractor.py

import os

def extract_vehicle_color(question: str) -> str:
    """
    Simulate extracting the color from user question (for dev mode).
    Replace this with real LLM call once you have the API key.
    """
    print("⚠️ Simulated LLM: Returning 'white' as default test color.")
    return "white"  # change as needed for testing
